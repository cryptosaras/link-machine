# Database
DB_PASSWORD=change-me-to-a-secure-password

# Security
JWT_SECRET=change-me-generate-64-char-random-hex
ENCRYPTION_KEY=change-me-generate-32-byte-base64-key

# OpenRouter (optional, can set in UI later)
OPENROUTER_API_KEY=
